@model IEnumerable<HoangTrungThong774.Models.HTT0774>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HTTName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HTTGender)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HTTName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HTTGender)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.HTTId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.HTTId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.HTTId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
